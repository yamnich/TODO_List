.content
  - if @project.members.nil?
    %p There is no member
  - else
    .well
      %table{class: "table"}
        - @project.members.each do |member|
          %tr
            %td
              = member.name
            %td
              =  link_to  "Remove user", destroy_members_project_path(@project, member), confirm: 'Are you sure?', method: :delete, class: "btn btn-primary"

  %p
    =  link_to "Invite user to project", project_path(@project)+'/members/invite', class: "btn btn-info"