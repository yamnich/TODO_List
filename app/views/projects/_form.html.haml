= form_for([@user,@project],class: "well") do |f|
  = render 'shared/error_messages', :object => f.object
  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_area :description, rows: 5
  .actions
    = f.submit @button_name, class: "btn-info"

